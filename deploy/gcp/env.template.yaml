POSTGRES_PASSWORD:
  secret: netdiscoverit-postgres-password
  version: latest

JWT_SECRET_KEY:
  secret: netdiscoverit-jwt-secret
  version: latest

INTERNAL_API_KEY:
  secret: netdiscoverit-internal-api-key
  version: latest

CREDENTIAL_ENCRYPTION_KEY:
  secret: netdiscoverit-credential-encryption-key
  version: latest

ANTHROPIC_API_KEY:
  secret: netdiscoverit-anthropic-api-key
  version: latest

OPENAI_API_KEY:
  secret: netdiscoverit-openai-api-key
  version: latest

GOOGLE_API_KEY:
  secret: netdiscoverit-google-api-key
  version: latest

NEO4J_PASSWORD:
  secret: netdiscoverit-neo4j-password
  version: latest

MINIO_SECRET_KEY:
  secret: netdiscoverit-gcs-secret-key
  version: latest
